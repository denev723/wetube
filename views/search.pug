extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 #{term}에 대한 검색결과
    .search__videos
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })