header.header
    .header__wrapper 
        .header__column
            a(href=routes.home)
                i.fab.fa-youtube.fa-2x
        .header__column
            form(action=routes.search, method="get")
                input(type="text", name="term", placeholder="검색어를 입력하세요..")
        .header__column
            ul
                if !user.isAuthenticated
                    li 
                        a(href=routes.join) 가입하기
                    li 
                        a(href=routes.login) 로그인
                else
                    li 
                        a(href=`/videos${routes.upload}`) 업로드
                    li 
                        a(href=routes.userDetail(user.id)) 프로필
                    li 
                        a(href=routes.logout) 로그아웃